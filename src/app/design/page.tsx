import { DocPage } from "@/components/DocPage";

export default function DesignNotePage() {
  return (
    <DocPage
      file="DESIGN.md"
      eyebrow="Design note"
      lede="The MVP, the domain model, where the deterministic and AI parts divide, and what I traded away to fit two days."
    />
  );
}
