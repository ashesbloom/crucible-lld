import { DocPage } from "@/components/DocPage";

export default function ResearchPage() {
  return (
    <DocPage
      file="RESEARCH.md"
      eyebrow="Research note"
      lede="Six existing tools, one method between them, and the question they all avoid because it does not have an answer."
    />
  );
}
